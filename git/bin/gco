#!/usr/bin/python

import os
import argparse

parser = argparse.ArgumentParser(description = 'alias git checkout "target"')
parser.add_argument('target', action = 'store', help = 'branch name or file name')
args = parser.parse_args()

os.system('git checkout "%s"' % args.target)
