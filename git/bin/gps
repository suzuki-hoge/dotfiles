#!/usr/bin/python

import os
import argparse

parser = argparse.ArgumentParser(description = 'git push origin --branch')
parser.add_argument('branch', action = 'store', help = 'branch name')
args = parser.parse_args()

os.system('git push origin "%s"' % args.branch)
