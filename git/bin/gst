#!/usr/bin/python

import os
import sys
import os.path
import argparse
import subprocess

sys.path.append(os.path.abspath('%s/../../' % os.path.abspath(__file__)))
import util

parser = argparse.ArgumentParser(description = 'alias git status -s -b')
args = parser.parse_args()

fetchLines = util.getStdoutList('git status -s -b')

for line in fetchLines:
	print line.rstrip()
