#!/usr/bin/python

import os
import argparse

import commands

parser = argparse.ArgumentParser(description = 'alias git clean -d -f')

os.system('git reset .')
os.system('git checkout .')

os.system('git clean -d -f')
