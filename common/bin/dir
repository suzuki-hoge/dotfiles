#!/usr/bin/python

import os
import argparse

parser = argparse.ArgumentParser(description = 'open finder at current path')
parser.add_argument('path', metavar = 'path', type = str, nargs = '?', default = '.', help = "path (default : '.')")
args = parser.parse_args()

os.system('open %s' % args.path)
