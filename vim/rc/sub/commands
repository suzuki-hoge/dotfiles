" vim:set ft=vim:

" edit todo
command! Todo tabe $todo

" edit blog-memo
command! Blog tabe $blog

" edit blog-draft
command! Draft tabe $draft

" message
command! M messages

" edit temporary file at noname buffer
command! -nargs=1 Tmp call FiletypeBuffer(<f-args>)

" redirect command result and output to noname buffer
command! -nargs=1 -complete=command Cap call Cap(<f-args>)
 
function! Cap(command)
	redir => result
	silent execute a:command
	redir END

	let bufname = 'Capture: ' . a:command
	call NonameBuffer(bufname, result)
endfunction

" toggle relative/absolute line number
command! Rel setlocal relativenumber!

" color highlight at console vi
command! Color syntax on | source $dot/vim/plugin/todo/todo.vim
