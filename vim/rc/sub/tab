" vim:set ft=vim:

" change tab
nnoremap <Space> gt
nnoremap <S-Space> gT

" new tab
"  Actual input is <C-Space>.
"  but <C-Space> was disabled by ATOK. (ref. private.xml)
nnoremap <D-Space> :tabedit 

" close tab
" --------
" is frank
" --------
nnoremap <C-Space>   :call CloseAllFrankOrBDelete()<CR>

function! CloseAllFrankOrBDelete()
	if IsFrank()
		call CloseAll()
	else
		execute 'bdelete'
	endif
endfunction

" close tab (force)
nnoremap <S-C-Space> :bdelete!<CR>
