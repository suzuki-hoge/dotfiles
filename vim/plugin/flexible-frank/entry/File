" vim:set ft=vim:

python <<EOM

class File(Entry):

	def __init__(self, root, path):
		Entry.__init__(self, root, path)

	def getOutputs(self, result):
		return result.append(self.createOutput())

	def pointSwitch(self, target):
		if self.num.isTarget(target):
			if self._isPointed():
				self.pointed = False
			else:
				self.pointed = True

EOM
