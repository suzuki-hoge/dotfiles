" vim:set ft=vim:
python <<EOM

class cQuick(cBase):

	def __init__(self, entry):
		self.entry = entry

	def do(self):
		func = lambda e: vim.command('silent !qlmanage -p %s' % e.path.value)
		func(self.entry)

	def undo(self):
		pass

EOM
