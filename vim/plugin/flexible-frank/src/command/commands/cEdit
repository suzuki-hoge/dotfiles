" vim:set ft=vim:
python <<EOM

class cEdit():
	def __init__(self, entries):
		self.entries = entries

	def do(self):
		func = lambda e: vim.command('tabedit %s' % e.path.value)
		[func(e) for e in self.entries]

	def undo(self):
		pass

EOM
