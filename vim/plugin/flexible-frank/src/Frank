" vim:set ft=vim:

python <<EOM

import vim
import os
import os.path

class Frank:

	def __init__(self, root):
		Num.init()
		root = Path(root)
		self.node = Directory(root, root)
		self.buffer = Buffer2()

	def output(self):
		self.buffer.output(self.node.getOutputs([]))

	def pointSwitch(self):
		self.node.pointSwitch(5)

EOM
