" vim:set ft=vim:

python <<EOM

import vim
import os
import os.path

class Frank:

	def __init__(self, root):
		self.node = Directory(root, root)
		self.buffer = Buffer2()

	def output(self):
		self.buffer.output(self.node)

	def dump(self):
		self.node.dump()
EOM
