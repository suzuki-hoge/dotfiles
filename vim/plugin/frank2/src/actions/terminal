" vim:set ft=vim:

function! N_TerminalOpen()
	let entry = GetOneByCursor()
	call s:terminalOpen(entry)
endfunction

function! s:terminalOpen(entry)
	execute '!open -a Terminal ' . a:entry.path
endfunction
